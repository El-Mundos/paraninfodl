# .env (solo si quieres cambiar algo del default)
SESSION_FILE=~/.paraninfo_session.json  # dónde guardar las cookies
LOGIN_TIMEOUT=180                        # segundos para hacer login
PAGE_DELAY=0                             # pausa entre páginas (0 = sin pausa)
